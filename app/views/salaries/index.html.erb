<p id="notice"><%= notice %></p>

<h1>Salaries</h1>

<table>
  <thead>
    <tr>
      <th>Actual salary</th>
      <%# <th>Current salary</th> %>
      <th>Employee</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @salaries.each do |salary| %>
      <tr>
        <td><%= salary.actual_salary %></td>
        <%# <td><%= @realsalary %></td> 
        <td><%= salary.employee.name %></td>
        <td><%= link_to 'see the current salary', salary %></td> 
        <%# <td><%= link_to 'Edit', edit_salary_path(salary) %>
        <%# <td><%= link_to 'Destroy', salary, method: :delete, data: { confirm: 'Are you sure?' } %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%# <%= link_to 'New Salary', new_salary_path %> 
